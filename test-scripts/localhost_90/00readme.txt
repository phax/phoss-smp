This folder contains a non-exclusive set of test scripts to test the REST interface via curl

All scripts assume that the default username "admin@helger.com" with the default password "password" is used

Each command file writes into the file "response-data"
